#cloud-config
#
passwd:
  users:
  - name: core
$SSH_AUTHORIZED_KEYS

storage:
  files:
  - path: /etc/hostname
    filesystem: "root"
    contents:
      inline: $K8S_node

networkd:
  units:
  - name: 20-dhcp.network
    content: |
      [Match]
      name=en*

      [Network]
      DHCP=yes
